<!--tdc/find/find.wxml-->
<view class="page">
  <view wx:for="{{newsList}}" wx:key="">
    <view id="{{item.id}}" bindtap="newsinfo">
      {{item.id}}
      {{item.title}}
    </view>
  </view>
</view>
