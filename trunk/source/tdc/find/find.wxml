<!--tdc/find/find.wxml-->
<view class="page">
  <view wx:for="{{newsList}}" wx:key="" class="find-item-view">
    <find-component title="{{item.title}}" logo_photo="{{serverHttps + item.logophoto}}" content="{{item.content}}" bindtap="newsinfo" data-id="{{item.id}}" />
  </view>
</view>
